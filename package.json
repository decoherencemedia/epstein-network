{
  "name": "epstein-network",
  "private": true,
  "scripts": {
    "build": "bash ./site/build.sh && curl -fsSL https://bootstrap.pypa.io/get-pip.py | python3 && python3 -m pip install boto3 && python3 scripts/generate_static_search_pages.py"
  }
}
